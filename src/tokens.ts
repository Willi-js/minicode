enum Tokens {
    FUNC,
    LET,
    RET,
    IF,
    EL,
    ELIF,
    FOR,
    OUT,

    PLUS,
    MINUS,
    MULTI,
    DEV,

    LEFT_BRACE,
    RIGHT_BRACE,

    LEFT_CURLY,
    RIGHT_CURLY,

    MORE,
    LESS,

    PIPE,

    COMMENT
}

export default Tokens;